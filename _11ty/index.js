const bsTable = require('./bsTable');
const aref = require('./aref');
const callout = require('./callout');
const figure_abs=require('./figure_abs');
const kdb = require('./kdb');
const question=require('./question');
const btcollapse=require('./btcollapse');
const coln=require('./coln');
const hamburger=require('./hamburger');

module.exports = {
    bsTable,
    aref,
    callout,
    figure_abs,
    kdb,
    question,
    btcollapse,
    coln,
    hamburger,
};